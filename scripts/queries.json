db.side_chains.aggregate(
[
    {$group: {_id: "$atom_mapped_smiles", count: {$sum: 1
            }
        }
    },
    {$match: {
            "count": {
                "$gt": 1
            }
        }
    }
]
)